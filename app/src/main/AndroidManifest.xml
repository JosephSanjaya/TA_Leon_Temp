<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.leon.su">

    <application
        android:name=".SUApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SU_App">
        <activity android:name=".presentation.ProductRegisterActivity"></activity>
        <activity android:name=".presentation.PriceUpdateActivity" />
        <activity android:name=".presentation.InvoiceActivity" />
        <activity android:name=".presentation.StockUpdateActivity" />
        <activity android:name=".presentation.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>